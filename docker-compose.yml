version: '3.8'

services:
  zenalyze:
    build: .
    container_name: zenalyze
    ports:
      - "3003:3000"
    env_file: .env  # ← Asegurar que está esto
    environment:
      NODE_ENV: production
    restart: unless-stopped