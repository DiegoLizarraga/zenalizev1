version: '3.8'

services:
  zenalyze:
    build: .
    container_name: zenalyze
    ports:
      - "3003:3000"
    env_file: .env  # ← ESTA LÍNEA ES CLAVE
    environment:
      NODE_ENV: production
      # Las variables del .env se cargarán automáticamente
    restart: unless-stopped